<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.civstudio</groupId>
    <artifactId>civstudio</artifactId>
    <version>0.9.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>civstudio</name>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Which simulation `mvn exec:exec` runs; override with -Dsim.main=com.civstudio.simulation.SmallOpenEconomy -->
        <sim.main>com.civstudio.simulation.HomogeneousEconomy</sim.main>
        <lombok.version>1.18.34</lombok.version>
        <spring.boot.version>3.3.5</spring.boot.version>
    </properties>

    <!--
      Spring Boot is used for the database persistence backend only (a pooled
      DataSource, Flyway migrations, JdbcTemplate). The simulation domain stays
      plain-constructed; see com.civstudio.app.SimApplication. Import the BOM so
      the JDBC/Flyway/Postgres versions are managed by the Boot release.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.3</version>
            <scope>test</scope>
        </dependency>
        <!-- Parses the name tables in src/main/resources (see eos.name). -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.18.2</version>
        </dependency>

        <!-- Database persistence backend: pooled DataSource (HikariCP) + JdbcTemplate. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <!-- Versioned schema migrations, run on startup. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <!-- Postgres JDBC driver. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!--
              Runs a simulation in a forked JVM with assertions enabled (-ea).
              The original code uses `assert` as real invariant checks, so we keep them on.
              Usage: mvn compile exec:exec   (HomogeneousEconomy by default)
                     mvn compile exec:exec -Dsim.main=com.civstudio.simulation.SmallOpenEconomy
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <executable>${java.home}/bin/java</executable>
                    <arguments>
                        <argument>-ea</argument>
                        <argument>-classpath</argument>
                        <classpath/>
                        <argument>${sim.main}</argument>
                    </arguments>
                </configuration>
            </plugin>

            <!--
              Each simulation test runs the full colony. Settlement state is
              per-instance now, but the SimLog logging handler is still a
              process-global static, so a fresh JVM is forked per test class
              (reuseForks=false) to isolate it. Assertions stay on, matching
              how the simulations run under exec.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <reuseForks>false</reuseForks>
                    <enableAssertions>true</enableAssertions>
                </configuration>
            </plugin>

            <!--
              Launches the Spring Boot app (com.civstudio.app.SimApplication) that
              runs a simulation with database persistence enabled. Assertions stay
              on (-ea), matching exec:exec and the test runs.
              Usage: mvn spring-boot:run
                     mvn spring-boot:run -Dspring-boot.run.arguments=-sim=SmallOpenEconomy
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <mainClass>com.civstudio.app.SimApplication</mainClass>
                    <jvmArguments>-ea</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
