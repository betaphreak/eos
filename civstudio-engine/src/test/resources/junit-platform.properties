# Parallel test execution (2026-07-23, the post-default-flip speedup): surviving colonies
# run their full 25 years, so the suite's long pole is a handful of INDEPENDENT full-length
# scenario runs — run test CLASSES concurrently (methods stay same-thread within a class).
# All sim state is per-instance/per-session by design (the Phase-0 de-globalization); the
# four scenario-main classes that write output/<seed>/ CSVs serialize among themselves via
# @ResourceLock("scenario-output"). Factor 0.5 leaves headroom — each scenario run is CPU-bound.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.config.dynamic.factor=0.5
