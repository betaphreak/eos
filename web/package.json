{
  "name": "civstudio-web-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the CivStudio web (build.mjs). The site itself is dependency-free; this holds only the build-time image encoder.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test js/*.test.mjs"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
